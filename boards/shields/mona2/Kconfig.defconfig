if SHIELD_MONA2_DONGLE || SHIELD_MONA2_R || SHIELD_MONA2_L

    config ZMK_KEYBOARD_NAME
        default "mona2"

    config NFCT_PINS_AS_GPIOS
        default y

    config ZMK_SPLIT
        default y
    
    config ZMK_POINTING
        default y
    
    config ZMK_MOUSE
        default y

    config PAW3222
        default y

    config INPUT
        default y
    
    config SPI
        default y


    config ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS
        default 2

endif